apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testing
spec:
  rules:
  - host: testing.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: testing
            port:
              number: 80
